---
- name: Firewall & Network Policies
  hosts: all
  become: yes
  gather_facts: true

  vars_files:
    - vars/common.yml

  roles:
    - firewall-setup
    - iptables-config
    - network-segmentation
