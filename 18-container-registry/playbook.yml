---
- name: Container Registry Management
  hosts: all
  become: yes
  gather_facts: true

  vars_files:
    - vars/common.yml

  roles:
    - registry-setup
    - image-scanning
    - lifecycle-mgmt
